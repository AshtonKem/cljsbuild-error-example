# Lein Cljsbuild Error Example

This demonstrates a bug between java-source-paths and lein-cljsbuild.

## Usage

See that the app runs with ```lein run```

Then try to use ```lein cljsbuild once``` and see the errors.
